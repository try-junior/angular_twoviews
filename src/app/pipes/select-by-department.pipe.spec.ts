import { SelectByDepartmentPipe } from './select-by-department.pipe';

describe('SelectByDepartmentPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectByDepartmentPipe();
    expect(pipe).toBeTruthy();
  });
});
