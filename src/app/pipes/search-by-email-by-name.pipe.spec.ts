import { SearchByEmailByNamePipe } from './search-by-email-by-name.pipe';

describe('SearchByEmailByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByEmailByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
